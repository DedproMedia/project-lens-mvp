export default function AuthLiveMarker() {
  return (
    <div style={{padding:20}}>
      <h1>AUTH LIVE MARKER ✅</h1>
      <p>If you see this at /auth, routing & deploy are correct.</p>
    </div>
  );
}
